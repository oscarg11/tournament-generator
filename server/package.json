{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dotenv -e .env.development -- nodemon server.js",
    "test:server": "dotenv -e .env.test -- nodemon server.js",
    "seed": "node scripts/seed.js default",
    "seed:knockout": "node scripts/seed.js knockout",
    "seed:knockout-only": "node scripts/seed.js knockout-only",
    "seed:custom": "node scripts/seed.js custom",
    "seed:clean": "node scripts/seed.js default --clean",
    "test:setup": "node scripts/seed.js knockout",
    "test:reset": "node scripts/seed.js default --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.2"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  }
}
